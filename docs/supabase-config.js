// ============================================================
// Supabase connection — fill in after creating your project:
// 1. Create a free project at https://supabase.com
// 2. Run docs/supabase-setup.sql in the SQL Editor
// 3. Copy from Project Settings → API:
//    - Project URL   → SUPABASE_URL
//    - anon public   → SUPABASE_ANON_KEY  (safe to expose in client)
// Leave both empty to run the site without accounts (everything
// else keeps working — auth UI simply stays hidden).
// ============================================================
window.SUPABASE_URL = '';
window.SUPABASE_ANON_KEY = '';
